// This file imports the test-error.ts file to demonstrate how the error overlay works
import { greet, username } from './test-error';

// Using the imported values
console.log(greet('World'));
console.log('Username:', username); 